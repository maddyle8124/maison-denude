# Status Log — Maison Denude Frontend

> Newest on top. History of what happened. Current state lives in `CONTINUITY.md`; decisions in `../frontend/decisions.md` (D-rows) + `../decision.md` (MD-rows).

---

## 2026-07-08 (Booking page UI redesign — full English + month-calendar picker) — pm-PASSED

- **Trigger:** user reviewed the live booking page and gave two directives: (1) the whole booking UI must be **full English** (the VN-over-EN bilingual pairs on every label were cluttered/unprofessional); (2) the slot picker — which dumped ~40 full-date buttons in a grid — was "a disaster"; keep the two-pane layout but redesign the right panel.
- **Design decision process:** built `main-dev/booking-mockups.html` — 3 live clickable layout directions in the Maison design language (A: horizontal date rail → time grid; B: month-calendar grid → time column; C: grouped list with inline time pills). User picked **B (month calendar)**. Logged as **D-BOOK-UI-01** (full-English, supersedes D-9 for the page UI only — invite body stays bilingual) + **D-BOOK-UI-02** (month-calendar picker) in `booking-calendar.md`.
- **Implemented (3 files):** (1) `content/booking.ts` — converted every `{vi,en}` bilingual object to flat English strings; added `picker.weekdays`/nav labels/`pickDatePrompt` for the calendar. (2) `components/ui/BookingForm.astro` — rewrote the picker as a 7-col Mon-first month-calendar grid: days with availability clickable + carry a monochrome availability dot, empty days dimmed+disabled, `‹ ›` month nav steps only across months that have slots, selecting a day reveals its times in a side column; full English; +07 string-slice parsing preserved (no viewer-tz conversion; `Date.UTC` used only for tz-neutral weekday/day-count math). (3) `pages/booking/cancel.astro` — stripped bilingual, full English.
- **Verification:** `astro check` 0 errors / 0 warnings; `npm test` 135/135; `npm run build` Complete. Live wrangler-dev e2e: getAvailability → slots, month calendar renders (7 cal-dow headers + cal-grid confirmed in served HTML, zero VN leftovers), booking Online → success + Meet link + cancel token, cancel round-trip freed slot, UTF-8 name `Nguyễn Thị Ánh` stored correctly. Test event cleaned off calendar.
- **pm gate:** first pass **FAILED** on invariant 2 — the availability dot used `var(--color-gold, #d8b78e)` but `--color-gold` was never defined anywhere (a raw literal disguised as a token). Root-cause fix (better than pm's suggested "define the token"): `design-system.md:68` forbids accent colours without Chi's approval, so the dot was made **monochrome** (`var(--color-text)` @ 0.55 opacity; inverts to `--color-bg` @ 0.7 on the active cell). Re-verified: **VERDICT: PASS** (pm independently re-ran astro check 0/0 + vitest 135/135, confirmed no `--color-gold` remains, confirmed monochrome choice honours design-system.md:68).
- **NOT yet committed/deployed** — lives in the working tree with the rest of the uncommitted calendar feature.

---

## 2026-07-08 (Booking calendar Phase 5 — live testing RESOLVED) — DONE

- **Invites confirmed working:** the earlier "invite not arriving" was `GCAL_SEND_UPDATES=none` (the safe compaction default) suppressing Google's guest emails. Flipped to `all`, rebuilt, booked live → invites DID arrive (just delayed a few min). Fetched the created event back from the Calendar API: 3 attendees attached, `needsAction`, Meet link, organizer=thieuxmaison. NOTE: the owner/organizer never gets an invite email for its own event (expected). The `sendUpdates=all` PATCH returns 200 and Google delivers to attendees.
- **`Ch? �nh` typo proven NOT an app bug:** it was Git-Bash-on-Windows curl mangling the Vietnamese CLI arg (Windows argv=cp1252) before it reached the Worker. Re-booked the same name via Node FormData (UTF-8-clean, the SAME path the real browser form uses) → Google stored & returned `Chị Ánh` perfectly (88 UTF-8 bytes). App is foundationally correct on encoding. Lesson: never use Git Bash curl for non-ASCII test data.
- **Branded-email question RESOLVED — user declined.** The Google invite's outer chrome (date block, Join button, RSVP) is Google's template, unstylable by any API; only the description body is ours. User decided the standard Google invite is sufficient; NO separate Resend branded email will be built.
- **Cleanup:** all 4 test events deleted off the thieuxmaison calendar (204). `.dev.vars GCAL_SEND_UPDATES` reset to `none`.

---

## 2026-07-08 (Booking calendar Phase 5 — OAuth setup + live integration + email-body polish) — IN PROGRESS (paused for compaction)

- **OAuth (D-10/D-12):** minted a Web OAuth client under **thieuxmaison@gmail.com** (the canonical 2-month project account — D-12; nguyenthaithieu@gmail.com is personal/test-invitee only). Google Cloud project `maison-denude`. Refresh token minted via OAuth Playground authorized-as thieuxmaison (that account owns the events). Cred file (git-ignored) `main-dev/env/maison-denude-booking.json`. Secrets recorded in git-ignored `.env` + NEW `.dev.vars`: GOOGLE_CLIENT_ID/SECRET/REFRESH_TOKEN, GOOGLE_CALENDAR_ID=GCAL_OWNER_EMAIL=thieuxmaison@gmail.com, GCAL_SEND_UPDATES=none (safe default). `.dev.vars` also mirrors SUPABASE_*/RESEND_* so wrangler dev has the full env.
- **Direct API probe PASSED:** refresh→access token exchange OK (scope `.../auth/calendar`); freeBusy OK; insertEvent w/ `conferenceDataVersion=1` → real Meet link; delete 204.
- **Full-app integration via `wrangler dev` on BUILT worker (loads .dev.vars) — ALL PASSED:** getAvailability → 405 slots, `freeBusyUnavailable:false` (live free/busy worked), first slot correctly +24h (13:00 next day, lead guard live), last 17:00 @60d; createBooking Online → success + real Meet + cancelUrl; booked slot removed from availability (405→404, DB-taken live); double-book same slot (different email) → `CONFLICT` 409 (atomic partial-unique index enforced through the full stack); cancel via token → event deleted + slot freed (→405); double-cancel → friendly no-op; calendar 0 active events after cleanup. Gotchas learned: (a) `astro dev` does NOT load .dev.vars / `cloudflare:workers` env — MUST `npm run build` then `wrangler dev`; wrangler dev does NOT auto-rebuild on source change. (b) Astro form-actions enforce an Origin CSRF check — curl needs `-H "Origin: http://127.0.0.1:8788"`. (c) action responses are devalue-serialized (index-map array), not plain JSON.
- **Live email send-test (GCAL_SEND_UPDATES=all):** real Google invites emailed to the 3 team addrs + client, with Meet link, bilingual body, all attendees, organizer=thieuxmaison (owner not a dup attendee). User-reported findings: (i) the CANCEL email arrived but not the INVITE — because test booked+cancelled seconds apart so Gmail collapsed to latest state; NOT a bug (real bookings sit before cancel); re-tested by leaving an event uncancelled. (ii) The event **description was an ugly plain-text debug dump** → **FIXED**: `calendar.ts buildEventPayload` description rewritten to on-brand bilingual HTML using Google's rendered subset (`<b>` brand+type, `<a href>` cancel link, `<br>`), greeting + client details (notes line omitted when empty) + Meet-or-address line; added `escapeHtml` for injection safety; `calendar.test.ts` updated + strengthened (asserts `<a href>`, `<b>Maison Denude</b>`, no empty notes line). 135/135 still green, astro check 0/0. (iii) The `�` was em-dash mangled by the test-harness curl, NOT an app bug (app stores UTF-8 correctly — proven by json.load round-trip; the crash was only Windows cp1252 console *printing*).
- **A live test event is CURRENTLY on the thieuxmaison calendar** (left for user inspection): 2026-07-09 13:00 Online "Chị Ánh", Meet `meet.google.com/euv-pwon-sus`, 3 team invitees, invite emailed. Needs cleanup next session.
- **REMAINING:** user confirms invite lands + body OK (offer separate Resend branded email if the Google chrome isn't enough — we only control the description, not Google's date-block/Join-button chrome); clean up the test event; set prod Worker secrets via `wrangler secret put` on thieuxmaison CF acct (+ GCAL_SEND_UPDATES=all for prod); promote spec DRAFT → D-BOOK-04 + re-mint runbook + residual double-book risk note; commit + deploy. ⚠ client_secret + refresh_token were pasted in chat — rotate post-launch.

---

## 2026-07-08 (Booking calendar Phase 4 — client slot-picker + bilingual content) — DONE (pm-PASSED)

- **Scope (general worker, then orchestrator recovery):** replace the date-only form with a live slot-picker, add required email + Online/Instore + rich bilingual confirmation + a `/booking/cancel` page. Worker HIT ITS SESSION LIMIT mid-run and never self-reported; orchestrator recovered its on-disk output.
- **`src/components/ui/BookingForm.astro`** — calls `actions.getAvailability({})` on mount, groups slots by +07 local date via STRING-SLICE of the `+07:00` ISO (never converts to viewer tz; `Intl.DateTimeFormat` pinned to `Asia/Ho_Chi_Minh` for weekday/month labels only), renders date chips → time chips → sets hidden `slotStart`; required `email` field; Online/Instore chip selector (hidden `consultationType`); submit guarded on slot chosen; confirmation shows Meet line (+fallback text if Online w/o meetUrl), ALWAYS the cancel link, pending message; `CONFLICT` → slot_taken msg + re-fetch grid; chips are real token-styled flat `<button>`s (`border-radius:0`, no shadow/gradient — D-BOOK-03 clean). **`src/content/booking.ts`** — all new copy as bilingual VN+EN pairs. **`src/pages/booking/cancel.astro`** (NEW, prerendered, `/booking/cancel`) — reads `?token` client-side → `actions.cancelBooking({token})` → friendly bilingual states.
- **Orchestrator fixes after worker died:** 4 `astro check` type errors — DOM `.append(vi,en)`/`.append(btn)` resolved to a wrong global (Response/stream) overload in the .astro client-script tsconfig context → switched to `.appendChild` (unambiguous Node signature); removed a dead `selectedDate` var (was write-only; selection tracked via `aria-pressed` + hidden input).
- **Verify (ACTUAL):** 135/135 tests green; `npx astro check` **0 errors / 0 warnings** / 18 pre-existing hints; `npm run build` Complete. **pm-PASSED** with file:line evidence (bilingual coverage, field names match action contract incl. required email + hidden slotStart guard, +07 string-slice grouping, confirmation branch logic, D-BOOK-03 chip styling, cancel page).

---

## 2026-07-08 (Booking calendar Phase 3 — orchestrator + actions wiring, TDD) — DONE, awaiting pm-VERIFY

- **Scope (general worker):** the crux orchestrator `booking-flow.ts` + test (written FIRST), plus wiring three actions in `src/actions/index.ts`. Failure contract implemented EXACTLY per plan `vivid-percolating-karp.md` §"Failure contract". Consumed the Phase-2 pm-passed modules unchanged (slots/availability/google-auth/calendar/supabase/email).
- **`src/lib/booking-flow.ts` (NEW)** — DB-agnostic orchestrator. `BookingStore` interface (all DB ops behind it: `claimSlot`/`findLiveBySlotAndEmail`/`markPending`/`storeEventId`/`releaseClaim`/`takenStarts`/`findByCancelToken`/`markCancelled`); `BookingFlowDeps` injects google/calendar fns (calendar-id bound at call site) + config; `bookSlot(input, deps)` returns discriminated `{status:'success'|'slot_taken'|'pending'|'error'}`, NEVER throws. Contract order: (1) `isBookable` lead/horizon re-check (D-7: 24h/60d consts exported) → `slot_taken`; get access token; re-query free/busy for JUST this slot via `subtractBusy` → busy ⇒ `slot_taken` (a free/busy READ failure does NOT block — DB unique index stays the gate, residual risk documented in plan); (2) idempotency short-circuit `findLiveBySlotAndEmail` → returns that row's success, no 2nd event; (3) atomic `claimSlot`: `slot_taken` reason ⇒ loser `slot_taken` (nothing created, no compensation), `error` reason ⇒ `error`; (4) token fetched BEFORE claim (needed for the free/busy re-check) — a token failure means no claim happened, nothing to compensate [DEVIATION, see below]; (5) `buildEventPayload` + `insertEvent`: ok ⇒ `storeEventId` (if it THROWS, log loudly + still return success — never delete a live event); definite 4xx (status 400/401/403/409/…) ⇒ `releaseClaim` compensate ⇒ `error`; indeterminate (status≥500 OR status===0 network/timeout) ⇒ `markPending` ⇒ `pending` (NOT deleted — deterministic requestId makes retry safe). `cancelBooking(token, deps)` (D-8): unknown token / already-cancelled ⇒ friendly no-op success; live row ⇒ `deleteEvent` (idempotent, skipped if no eventId, best-effort) then `markCancelled` (frees slot — 'cancelled' outside the unique-index predicate); NEVER throws (a store failure still resolves success). `cancelUrl = ${cancelBaseUrl}?token=<token>`.
- **`src/lib/booking-store.ts` (NEW)** — the Supabase-backed `BookingStore` (kept in its own file; the flow stays fake-tested). `claimSlot` INSERTs status='confirmed' with a `crypto.randomUUID()` cancel_token, maps Postgres `error.code === '23505'` → `reason:'slot_taken'`, any other error → `reason:'error'`. `storeEventId` throws on DB error so the flow takes its "event live, row not updated → log+success" path. All reads/writes via the passed service-role client (invariant 6). This is the ONLY place raw booking SQL lives for this feature.
- **`src/actions/index.ts` (REWRITE)** — three actions on the existing `export const server = {}`: **`getAvailability`** (QUERY action, no `accept:'form'`, JSON): builds `WeeklyRules` from D-6 consts, `generateSlots(now, 24, 60)`, `store.takenStarts` (service-role), best-effort free/busy over the horizon, `computeAvailability`; on ANY Google failure DEGRADES — returns slots minus DB-taken + `freeBusyUnavailable:true` so the page isn't dead. **`createBooking`** (keeps `accept:'form'`): new Zod input `name/phone/email(.email())/slotStart/consultationType('Online'|'Instore')/notes?`; derives slotEnd (+60m, re-serialized +07); wires real store + flow deps (D-5 team invitees hardcoded, ownerEmail/sendUpdates/cancelBaseUrl from env with defaults — `sendUpdates='all'` only if `GCAL_SEND_UPDATES==='all'` else `'none'`); maps result → `success`⇒`{success,meetUrl,cancelUrl}`, `pending`⇒`{success,pending:true,cancelUrl}`, `slot_taken`⇒ `ActionError('CONFLICT')`, `error`⇒ `ActionError('INTERNAL_SERVER_ERROR')`. KEEPS best-effort `sendBookingNotification` after success/pending (passes the human slot label as its `date` field — NO email.ts change needed, its shape already `Online|Instore` from Phase 1). **`cancelBooking`** (NEW query action): `{token}` → flow cancel → always `{success:true}`. Local `BookingEnv` interface (extends GoogleAuth/Calendar/Supabase/Email envs + `GCAL_OWNER_EMAIL`/`GCAL_SEND_UPDATES?`/`PUBLIC_SITE_URL?`), cast `env as unknown as BookingEnv`; zero import-time env.
- **Tests written FIRST** (`booking-flow.test.ts`, 19 tests — in-memory `BookingStore` fake with call records + stub google fns, no `vi.mock`, mirrors email/calendar style): Online success (meetUrl) / Instore success (location, no conferenceData, no meetUrl) / sendUpdates passthrough / free/busy-busy⇒slot_taken-no-claim / past-lead⇒slot_taken / idempotency⇒success-insertEvent-not-called / claim-23505⇒slot_taken-no-insert / claim-generic⇒error / token-fail⇒error-no-orphan-claim / insert-403⇒releaseClaim-error / insert-500⇒markPending-pending-no-release / insert-network(0)⇒pending / storeEventId-throws⇒success-event-not-deleted / claimSlot-throws⇒error (never throws out) / cancel valid⇒deleteEvent+markCancelled / cancel unknown⇒no-op / cancel double⇒no-op / cancel no-eventId⇒markCancelled-no-delete / cancel store-throws⇒friendly success.
- **Verify (ACTUAL):** `npx vitest run` → **6 files, 135 passed, 0 failed** (116 prior all still green + 19 new). `npx astro check` (installed `@astrojs/check` this run; it was not previously wired) → **0 errors** across 53 files; the action file typechecks. Remaining non-blocking: 1 deprecation warning on `z.string().email()` (the plan mandates `.email()`; `z.email()` triggers a WORSE `astro:schema` re-export deprecation cascade, so `.string().email()` kept), plus pre-existing unrelated Seo.astro/Base.astro hints.
- **DEVIATION (documented, needs pm review):** the plan's contract fetches the access token as step 4 (after the claim) and compensates `releaseClaim` on token failure. But step 1 also requires a free/busy re-query, which NEEDS a token. Resolved by fetching the token ONCE before the free/busy re-check (hence before the claim) — strictly safer: a token failure now returns `error` with NO orphan claim to compensate. The corresponding test asserts `claimSlot` NOT called (0 releases) rather than claim-then-release. Functionally equivalent end state (no confirmed-without-event), fewer wasted DB writes.
- **Cross-scope note:** email.ts / email.test.ts NOT touched — its `BookingNotification.consultationType` was already `Online|Instore` from Phase 1, so the new call only adapts the `date` field to a human slot label. No email.ts edit required.
- **Out of scope, not done (Phase 4):** client slot-picker in `BookingForm.astro`, bilingual `booking.ts` content, cancel landing route. Env secrets (`GOOGLE_*`, `GCAL_OWNER_EMAIL`) not yet set — live path unusable until Phase 0/5 OAuth.

---

## 2026-07-08 (Booking calendar Phase 2 — Google auth + calendar modules, TDD) — DONE, awaiting pm-VERIFY

- **Scope (general worker):** two pure/injectable modules + their tests, no wiring into actions, zero import-time env, injectable `fetch`, NO SDKs — per plan `vivid-percolating-karp.md` "Module decomposition" (google-auth/calendar signatures + "Edge cases baked into the tests") and booking-calendar.md D-3/D-5/D-9/D-10/D-11. Companion to the parallel worker's slots/availability entry below.
- **`src/lib/google-auth.ts`** — `GoogleAuthEnv` (CLIENT_ID/CLIENT_SECRET/REFRESH_TOKEN); `resolveGoogleAuth(env)` pure passthrough; `getAccessToken(cfg, fetchImpl?)` POSTs **form-urlencoded** (`application/x-www-form-urlencoded`, NOT JSON) to `https://oauth2.googleapis.com/token` with `grant_type=refresh_token` + client_id/secret/refresh_token. 200 → `{ok:true, accessToken, expiresIn}`; non-2xx (incl. 400 `invalid_grant` = revoked/expired token, status embedded in error) / network reject → `{ok:false, error}`, NEVER throws; missing any of the three config values → `{ok:false}` and fetch NOT called.
- **`src/lib/calendar.ts`** — pure `meetRequestId(slot)` = `md-<slot.startIso>` (deterministic, no Date.now/Math.random → insert retry reuses same Meet requestId); pure `buildEventPayload(input)`: start/end `{dateTime, timeZone:'Asia/Ho_Chi_Minh'}`, bilingual VN+EN summary+description (client name/phone/notes + cancelUrl, plain text, angle brackets stripped), attendees = dedupe([...teamInvitees, clientEmail]) **minus ownerEmail** case-insensitively (D-5; owner is organizer not attendee), Online → `conferenceData.createRequest{requestId, conferenceSolutionKey.type:'hangoutsMeet'}` (D-3), Instore → `location=atelierAddress` + no conferenceData. Injected I/O (all never-throw, discriminated results): `queryFreeBusy` (POST /freeBusy, parses `calendars[cal].busy` → `BusyInterval[]`, defensive on missing key), `insertEvent` (POST events?`conferenceDataVersion=1`&sendUpdates=..., URL-encoded calendarId, extracts `id` + meetUrl from hangoutLink OR video entryPoint — meetUrl may be undefined; non-2xx → `{ok:false,status,error}`), `deleteEvent` (idempotent — 2xx OR 404 OR 410 → `{ok:true}`). `resolveCalendarEnv(env)` passthrough.
- **`calendar.ts` sibling types:** imports `Slot`/`BusyInterval` via `import type` from `./slots`/`./availability` (type-only, erased by esbuild at runtime) — so the run is not blocked even though those files landed in the parallel worker's scope; tests define the structural shapes locally.
- **Tests written FIRST** (mirroring `email.test.ts`: no `vi.mock`, table-driven, `vi.fn(async()=>new Response(...))`, assert on `.mock.calls`; fixed sample slot `2026-07-15T10:00:00+07:00`→`11:00:00+07:00`; fully deterministic): `google-auth.test.ts` (13 — form-encoded endpoint/body assertion, 200 success, no-throw on 400 invalid_grant/401/403/429/500/503 + network reject, missing-config-no-fetch ×4) + `calendar.test.ts` (41 — deterministic requestId equality, Online conferenceData vs Instore location, attendees dedupe + owner-excluded case-insensitively + client==team no-dup, bilingual VN+EN description + cancelUrl + no-HTML-injection + null/undefined notes, freeBusy parse/empty/defensive/errors, insertEvent hangoutLink vs entryPoint vs missing + status errors + reject, deleteEvent idempotent 404/410 + other errors). One test-only fix: 204 delete response uses `new Response(null,{status:204})` (a 204 body must be null).
- **Verify:** `npm test` (full suite) → **5 files, 116 passed, 0 failed**. Per-file confirmed by isolated runs: `email.test.ts` 22 (pre-existing, untouched), `google-auth.test.ts` 13, `calendar.test.ts` 41 (+ parallel worker's slots 23 + availability 17). Both google-auth and calendar import cleanly now.
- **Deviations:** none from scope. Did NOT create slots.ts/availability.ts (parallel worker's scope) or wire anything into `src/actions/index.ts` (Phase 3).

---

## 2026-07-08 (Booking calendar Phase 2 — pure/injectable slot + availability core, TDD) — DONE, awaiting pm-VERIFY

- **Scope (general worker):** two pure modules + their tests, no wiring/I/O/env, per plan `vivid-percolating-karp.md` "Module decomposition" and booking-calendar.md D-2/D-6/D-7/D-11.
- **`src/lib/slots.ts`** — `WeeklyRules`/`Slot` types; `toLocalIso(y,mo,d,h,min,offsetMinutes)` builds RFC-3339 with a literal offset (e.g. `2026-07-15T10:00:00+07:00`), zero-padded, correct sign/HH:MM; `isBookable(slot,now,minLeadHours,maxHorizonDays)` epoch-based, inclusive both ends (`>=` lead, `<=` horizon); `generateSlots(rules,now,lead,horizon)` iterates local +07 calendar days (D-6: Mon–Sat 10:00–17:00 starts, closed Sun, fixed UTC+7 no DST), emits 60-min slots, trims via `isBookable`. Local-day identity derived by shifting the instant into +07 and reading UTC getters, then composing each candidate via `toLocalIso` and parsing back for instant comparison — never adds UTC ms across the offset.
- **`src/lib/availability.ts`** — `subtractBusy` (strict overlap `slotStart < busyEnd && slotEnd > busyStart` → adjacency is NOT overlap: busy 10–11 removes 10:00 but keeps 11:00), `subtractTaken` (matches by epoch instant, so a `Z` taken start removes the same-instant +07 slot), `computeAvailability = subtractTaken(subtractBusy(...))` (D-2 pipeline). All epoch-based; both busy `Z` strings and +07 slot strings parsed to ms. Inputs not mutated.
- **Tests written FIRST** (mirroring `email.test.ts`: no `vi.mock`, table-driven, fixed injected `now = 2026-07-14T00:00:00+07:00`): `slots.test.ts` (23 tests — toLocalIso padding/offset/instant, lead boundary 23h-out/24h/25h, horizon day-60/day-61, closed-Sunday, first-10:00/last-17:00-never-18:00, 8 slots/open-day, determinism) + `availability.test.ts` (17 tests — adjacency≠overlap, partial overlap, Z↔+07 matching for busy AND taken, both-busy-and-taken removed once, empty cases, no-mutation).
- **Verify:** `npx vitest run src/lib/slots.test.ts src/lib/availability.test.ts src/lib/email.test.ts` → **3 files, 62 passed** (22 pre-existing email untouched + 40 new). Per-file: slots 23, availability 17.
- **Note (out of scope, not touched):** full `npm test` also picks up `src/lib/google-auth.test.ts` — a parallel worker's Phase-2 file whose implementation `./google-auth` hasn't landed yet, so that ONE suite fails to import. Not my scope (Google modules owned in parallel); my three suites are fully green in isolation.

---

## 2026-07-03 (D-ARCH-02/D-HERO-01/D-BRAND-02 — static-first architecture + favicon + hero logo fix) — DONE, deployed, pm-PASSED

- **Root cause found:** `astro.config.mjs` carried a manual `image.service.entrypoint: 'astro/assets/services/compile'` — that module path does not exist in Astro 7, so `astro:assets` silently fell back to a passthrough service and shipped original files unoptimized, including one 18MB JPEG live on the homepage.
- **Fix (D-ARCH-02):** removed the bogus manual `image.service` entry; image optimization now comes from the adapter option `adapter: cloudflare({ imageService: 'compile' })` (sharp at build time). `output` stays `'server'` (needed only so the injected `/_actions/*` endpoint can run). All 5 pages (`/`, `/about`, `/collections`, `/booking`, `/404`) declare `export const prerender = true` (invariant 4 held) — `/booking` flipped back from `false` to `true`, with an inline comment explaining the client-side Astro Actions architecture that makes this safe (form no longer relies on SSR `Astro.getActionResult`; it now posts via `actions.createBooking(new FormData(form))` client-side to the always-server-rendered `/_actions/*` route).
- **`BookingForm.astro` rewritten accordingly:** no more `Astro.getActionResult`; renders the form + a hidden success block, and a client `<script>` imports `actions` from `astro:actions`, submits `FormData`, toggles the success/error blocks, and shows a sending-state on the submit button (`sendingLabel` from content). Added `.booking[hidden] { display: none }` to override the component's own `display:flex` (which otherwise beats the UA `[hidden]` rule and left both states visible).
- **`src/content/booking.ts`:** added `sendingLabel: 'Sending…'` (invariant 1 — copy lives in the content module, not hardcoded in the component).
- **Favicon (D-BRAND-02):** new set generated from the client-supplied `C:\maison\favicon.png` (1080×1080) — `public/favicon.ico` (16/32/48), `favicon-96.png`, `apple-touch-icon.png` (180), `icon-192.png`, `icon-512.png`. Placeholder `public/favicon.svg` deleted. `Base.astro` head updated: `<link rel="icon" href="/favicon.ico" sizes="48x48">`, `<link rel="icon" type="image/png" href="/favicon-96.png" sizes="96x96">`, `<link rel="apple-touch-icon" href="/apple-touch-icon.png">`. Note: `icon-192.png`/`icon-512.png` exist in `public/` but are not yet referenced by any head link or manifest — harmless (unused static files), flagged as a minor loose end, not a decisions.md violation (D-BRAND-02 doesn't mandate a manifest).
- **Hero logo (D-HERO-01):** `.hero__logo` width changed from the regressed `2184px` (actually found as `1176px` in the pre-image diff, still full-bleed-scale) to `70%` on desktop; mobile untouched at `240px`. Inline comment cites D-HERO-01.
- **Verify (re-run by pm, not just trusted from the report):** `npm run test` → 1 file, **23/23 passed**. `npm run build` → exit 0, 99 optimized image assets generated; the specific 18MB source (`kol-black-sequin-aodai-rooftop-night.jpg`) now emits webp variants from ~6KB up to **344,778 bytes (≈336kB) at its largest requested width** — confirms the root-cause fix actually shrinks the flagged asset, not just a config comment. Grepped `astro.config.mjs` — no stray `image.service.entrypoint` remains, only the explanatory comment. All 5 pages confirmed via grep to declare `prerender = true`. `git diff` confirms `Hero.astro`'s only change is the `width: 1176px` → `width: 70%` swap (plus the D-HERO-01 comment) — no other regressions. `public/` confirmed to contain the 5 new favicon files and no `favicon.svg`.
- **Deployed:** version `360c16fb-6234-457d-8633-0d82e6a05d48` on Worker `maisondenude-web`. Live-verified (per orchestrator's report, spot-checked structurally consistent with the above): all routes 200 on maisondenude.com, favicon set 200, homepage TTFB 116ms, webp srcsets present in live HTML, hero logo `width:70%` present in live CSS. Live browser booking flow (Playwright): submit → THANK YOU success state → Supabase row (Vietnamese diacritics intact) → test row deleted; booking notification email fired to `thieuxmaison@gmail.com`.
- **Not committed to git** — this and prior uncommitted session work remain in the working tree; committing/pushing is the user's call (open action, see CONTINUITY.md).
- **pm VERIFY (2026-07-03): PASS.** Verified against `decisions.md` D-ARCH-02, D-HERO-01, D-BRAND-02, and `sitemap/_index.md`'s architecture pointer. All six claimed file-level changes read in full and matched against documented intent; `npm run test` and `npm run build` re-run directly by pm (not trusted from the report) — both green; the 18MB-JPEG-to-336kB-webp claim independently confirmed by inspecting `dist/client/_astro/` output sizes for that specific source file. One minor loose end noted above (unwired `icon-192.png`/`icon-512.png`) — logged as a non-blocking open item, not a fail condition.

---

## 2026-07-03 (D-NAV-02 — nav fix + /about page + CRITICAL booking-submission bug fix) — DONE, deployed, live-verified

- **Nav (D-NAV-02):** `nav.ts` now HOME (/) · ABOUT US (/about) · COLLECTION (/collections) · BOOKING (/booking) — all absolute routes. Previous anchor hrefs (`#introduction`) broke off-homepage (applied to the current page). INTRODUCTION renamed ABOUT US (FQ-01 provisionally resolved as maison-story page).
- **New /about page:** `src/content/about.ts` (all copy) + `src/pages/about.astro` (prerendered; eyebrow → "THE MAISON" display title → 3 short paragraphs [name meaning, founder Chi Bui + bespoke positioning, atelier address] → address block → portrait `renMuseEasternForest` → reused ClosingCta → Footer). Desktop two-column (text left, portrait right, right padding clears nav rail); mobile stacked. seo.ts gained `pages.about` (+breadcrumb); sitemap.xml now 4 routes.
- **CRITICAL bug found & fixed — real booking submissions were being LOST in production:** the form's progressive-enhancement action posts to the page URL (`?_action=createBooking`), but /booking was PRERENDERED → the POST hit the static-assets layer → **405, no insert, no email**. All prior "live verified" booking tests had bypassed the browser flow by POSTing directly to `/_actions/createBooking`. Fix: `/booking` is now **SSR** (`prerender = false`, declared intent per invariant 4) so POST → server render → styled THANK YOU success state. Also fixed a deploy trap found while debugging: a running `astro preview` locked `dist/`, so `astro build` silently kept a stale `dist/client/booking/index.html` shadowing the SSR route — kill preview servers (or `rm -rf dist`) before deploying.
- **Email-encoding concern (user's Gmail screenshot with `�`):** investigated — NOT a code issue. The mangled subjects/sender were from orchestrator's Windows-terminal curl smoke tests (shell mangles UTF-8 at submission). Proof: headless-browser submission with "Nguyễn Thị Ánh — kiểm tra dấu" → Supabase row and Resend subject both byte-perfect, email `delivered`.
- **Verified live:** all 5 routes 200 (/, /about/, /collections/, /booking/, sitemap); browser-submitted booking → THANK YOU state → row confirmed → email delivered; test rows deleted. Deployed version `410fa3bc-746f-43b7-9276-a8a36c94ff8a`.

---

## 2026-07-03 (D-BOOK-03 — booking page UX/UI redesign, orchestrator) — DONE, deployed, live-verified

- **Design:** two-pane editorial layout. Desktop ≥1024px: sticky full-height cover image left (44vw), invitation panel right (eyebrow `Maison Denude — SAIGON` → display title `PRIVATE CONSULTATION` at `--text-booking-title` → lede → hairline rule → form → address/response meta). Mobile: 46vh cover banner, stacked panel, full-width submit. One staggered CSS-only entrance reveal, disabled under `prefers-reduced-motion`.
- **Cover asset:** IG export post 011 slide `_004` (blush metallic-lace áo dài, antiqued gold mirror — full-bleed editorial, no white frame) → `src/assets/booking/cover-blush-metallic-lace.jpg`, ImageKey `bookingCover`.
- **Form restyle (`BookingForm.astro`, presentation only — action/logic untouched):** numbered labels (01–05) from new content module, date + consultation share a row ≥640px, custom select caret, refined focus transitions, full-width submit that inverts (white bg/black text) on hover/focus, elevated success state (THANK YOU + rule + body + link to /collections), error styled as hairline-left note.
- **Content module:** new `src/content/booking.ts` — all page + form copy (invariant 1); `booking.astro` and `BookingForm.astro` are now render-only.
- **Tokens:** 5 added under `Booking page redesign (added D-BOOK-03)` comment: `--booking-pane-width`, `--booking-form-max`, `--booking-field-gap`, `--text-booking-title`, `--booking-cover-mobile-h`.
- **Verified:** build exit 0; 23/23 tests green; Playwright screenshots (1440px + 390px) visually reviewed by orchestrator against design-system.md; deployed (version `9a0ede19-2ac6-47b4-a790-71405ff54117`); live checks — `/booking/` 200 with cover asset present, live POST → `{"success":1}` → Supabase row confirmed via REST (then deleted) → Resend email **delivered** to thieuxmaison@gmail.com.
- Orchestrator-owned design work (D-BOOK-03); visual sign-off by orchestrator in lieu of pm gate for aesthetics; mechanical invariants (tokens-only styles, content module, prerender) held.

---

## 2026-07-03 (P6 — final QA, production deploy, live smoke test)

**What changed:** Closed out P6 for the Maison Denude website session. Sequence:
1. Pre-flight: confirmed the live Cloudflare Worker is `maisondenude-web` (old `main-dev` worker no longer exists), so the local `wrangler.jsonc` rename matches prod and the domain binding is safe. Confirmed Supabase secrets already present on the worker.
2. **Bug found/fixed — Hero.astro scoped-style break:** uncommitted WIP had passed `class` into `SmartImage`, which broke Astro's scoped styles (verified in built HTML: the `img` carried `SmartImage`'s `data-astro-cid` instead of Hero's, so there was no full-bleed positioning and no desktop/mobile image switching — silently reverting the fix from commit `97528d3`). Orchestrator restored the wrapper-div structure (`.hero__img--desktop` / `.hero__img--mobile`) plus the `.hero__img :global(img)` fill rule. Kept the user's deliberate logo width change (1176px to 2184px) — flagged to the user as a possible crop risk on smaller desktop viewports. Verified fixed in both the local build output and the live HTML (both wrappers carry `data-astro-cid-yodha2z4`).
3. Pushed secrets to the `maisondenude-web` worker via `wrangler secret put`: `RESEND_API_KEY`, `BOOKING_NOTIFY_EMAIL`.
4. Deployed twice (`npm run deploy`); final live version `d418c900-c772-41ab-b56f-09069e996a9d`.
5. **Bug found/fixed — Resend sender domain:** live smoke test hit a real bug — Resend rejected sends with HTTP 403 because `contact.maisondenude.com` is not a verified sending domain on the Resend account. `GET /domains` showed the verified domain is the apex `maisondenude.com` (verified 2026-07-03 10:01, region ap-northeast-1). Corrected `BOOKING_SENDER` in `main-dev/src/lib/email.ts` to `Maison Denude <contact@maisondenude.com>`, updated the matching expectation in `main-dev/src/lib/email.test.ts`, and corrected decision row D-BOOK-02 in `context/frontend/decisions.md`. Full test suite green (23/23) after the change.
6. **Live smoke test results** (all against `https://maisondenude.com`): `/`, `/collections/`, `/booking/`, `/robots.txt`, `/sitemap.xml` all returned 200 (sitemap was a transient 404 pre-deploy due to edge caching, confirmed 200 after); an unknown route served the branded 404 page ("Page Not Found"); homepage JSON-LD parsed correctly live with `@type` `[Organization, ClothingStore]`, 4 `sameAs` entries, 6 `subjectOf`, 7 `mentions`, founder Chi Bui; a live booking POST returned `{"success":1}`, confirmed as a new row in Supabase via MCP, and the notification email showed status **delivered** to `thieuxmaison@gmail.com` from `Maison Denude <contact@maisondenude.com>`. The 3 smoke-test rows were deleted from the `bookings` table afterward.
7. **Watch item logged (not yet actioned):** Cloudflare's zone-level "content signals" feature prepends a managed `robots.txt` block disallowing GPTBot/ClaudeBot/CCBot etc. (`ai-train=no`) ahead of our own file; our `Allow: /` + `Sitemap:` line is still intact below it. This is a dashboard toggle at the zone level — needs review if AI-engine discovery/visibility matters for the entity strategy in `context/SEO/03_entity_mentions.md`.
8. Still open for the client: OQ-001 (swap `BOOKING_NOTIFY_EMAIL` to a real team inbox), unclaimed Google Business Profile / Knowledge Graph entity (top action per `context/SEO/03_entity_mentions.md`), no Zalo URL yet (social icon removed from footer until one is provided).

**Why:** P6 was the final gate before go-live — confirm the deployed artifact matches documented intent (site structure, SEO/JSON-LD, booking flow) under real production conditions (real domain, real email delivery, real DB), not just local build output.

**Verification result:** PASS (self-verified by orchestrator via live HTTP checks, live JSON-LD parse, live Supabase row via MCP, live Resend delivery status; test suite 23/23 green). PM doc-CRUD pass performed 2026-07-03: spot-checked `email.ts` BOOKING_SENDER, `email.test.ts`, decisions.md D-BOOK-02/D-BOOK-03 rows, and Hero.astro wrapper/`:global(img)` structure against this log entry — all confirmed present in the artifacts as described above.

---

## 2026-07-03 — D-SEO-01 SEO foundation + entity-claiming JSON-LD sitewide — pm PASSED

- **Config:** `astro.config.mjs` `site: 'https://maisondenude.com'` verified already present (set by the killed prior session) — NOT edited.
- **Data module (new):** `src/content/seo.ts` — single source of truth for ALL SEO data (D-ARCH-01: URLs as data, never inline in markup). Contains: `sameAs` (4 official profiles), `subjectOf` (6 verified press URLs), `mentions` (7 press-tier candidates) — all copied VERBATIM from `context/SEO/03_entity_mentions.md` "Recommended JSON-LD arrays"; `org` entity facts (name Maison Denude / alternateName Maison Denude, PostalAddress 194 Lê Thánh Tôn 2nd Floor · District 1 · Ho Chi Minh City · VN, founder Person Chi Bui (Creative Director), foundingLocation HCMC, logoPath `/logo_white.svg`, knowsAbout = lawful vocabulary only); `site` config (siteName, locale en_US, `ogImageKey: 'renHeroWhiteJacquard'`); per-page `pages` map (`/`, `/collections`, `/booking`, `/404`) with EN titles/descriptions (~150 chars), breadcrumbs for collections/booking, `noindex: true` for 404; `getPageSeo(pathname)` lookup (trailing-slash tolerant).
- **Component (new):** `src/components/seo/Seo.astro` — rendered once in Base.astro's head; emits title + meta description (moved from Base, DRY), canonical (`Astro.site` + pathname; SKIPPED on noindex pages), OG (site_name/title/description/type=website/url/locale=en_US/image), Twitter (summary_large_image + title/description/image), and 4 JSON-LD blocks via `JSON.stringify` + `set:html`: (1) `["Organization","ClothingStore"]` `@id …/#organization` with sameAs/subjectOf/mentions (mentions included per D-SEO-01's "sameAs + subjectOf/mentions" — decisions win over the narrower spawn field list), NO fabricated ratings/reviews; (2) `WebSite` `@id …/#website` with publisher → org; (3) per-page `WebPage` → isPartOf website + about org; (4) `BreadcrumbList` on /collections + /booking.
- **OG image choice:** the Renaissance collection hero JPG (`renHeroWhiteJacquard` via `getAsset().src`, absolute via Astro.site) — chosen over the logo because og:image must be raster (SVG unsupported by most crawlers) and an editorial garment photo previews better.
- **Wiring:** `Base.astro` — imports Seo + seo.ts, defaults title/description to `pages.home`, replaces inline `<title>`/`<meta name=description>` with `<Seo>`; the 4 pages (`index/collections/booking/404.astro`) now pass their `pages.*` entries — no other page changes.
- **robots/sitemap (new, in `public/`):** `robots.txt` (allow all + Sitemap line); hand-written `sitemap.xml` with 3 routes, lastmod 2026-07-03. **Deliberate deviation-by-design (flagged in spawn):** no `@astrojs/sitemap` dependency for 3 routes — revisit when blog lands. Sitemap locs use trailing-slash form (`/collections/`, `/booking/`) to match canonicals (Cloudflare static-asset auto-trailing-slash); breadcrumb item URLs normalized the same way.
- **Verify:** `npm run build` exit 0 (4 routes prerendered). Node script over `dist/client/`: every `application/ld+json` block `JSON.parse`s clean on all 4 pages; org node has exactly 4 sameAs + 6 subjectOf + 7 mentions + correct `@id` everywhere; canonical + full OG/Twitter set on `/`, `/collections/`, `/booking/`; BreadcrumbList present on collections+booking with trailing-slash item URLs; 404 has `noindex` and NO canonical; robots.txt + sitemap.xml present in `dist/client/`. `npm run test` → 23/23 green. Brand-language grep over all new files: zero "high end"/"tailor" hits (only the comment stating the law; knowsAbout uses "Tailoring craft" per the canonical vocabulary).
- **Not touched (per scope):** Hero.astro WIP, wrangler.jsonc, nav/landing/collections content, booking logic, GTM, deploy.
- **pm VERIFY (2026-07-03): PASS.** Verified against `decisions.md` D-SEO-01 + D-ARCH-01, `context/SEO/03_entity_mentions.md` "Recommended JSON-LD arrays", and `context/SEO/01_seo_landscape.md` positioning. `src/content/seo.ts` read in full: `sameAs`/`subjectOf`/`mentions` arrays diffed URL-by-URL against the report's fenced blocks — byte-for-byte match, no typos, no extra/missing entries (4/6/7). Org facts confirmed: name "Maison Denude", alternateName "Maison Denude", address "194 Lê Thánh Tôn, 2nd Floor / District 1 / Ho Chi Minh City / VN", founder "Chi Bui" (Creative Director, consistent with `brand.md`), `logoPath` `/logo_white.svg`. `src/components/seo/Seo.astro` read in full: canonical/OG/Twitter all present; 4 JSON-LD blocks (`["Organization","ClothingStore"]` @id `.../#organization`, `WebSite` @id `.../#website` with `publisher`→org, per-page `WebPage`, `BreadcrumbList` on collections+booking only) — no fabricated `AggregateRating`/`Review`/`offers` anywhere (grepped, zero hits). `Base.astro` renders `<Seo>` once; all 4 pages (`index/collections/booking/404.astro`) pass their `pages.*` entries; `404.astro` uses `pages.notFound` (`noindex: true`). `public/robots.txt` confirmed (allow-all + Sitemap line); `public/sitemap.xml` confirmed (exactly 3 routes, trailing-slash locs matching canonicals). **Ran `npm run build` myself — exit 0, 4 routes prerendered.** Node script over `dist/client/*.html`: every `application/ld+json` block `JSON.parse`s clean on all 4 pages; org node has **exactly 4 sameAs + 6 subjectOf + 7 mentions** on every page; canonical present (with trailing slash) on `/`, `/collections/`, `/booking/`, absent on `/404.html`; `/404.html` has `<meta name="robots" content="noindex">` and no canonical; `og:image` on all 4 pages resolves to an absolute raster URL (`https://maisondenude.com/_astro/hero-white-jacquard-aodai.*.jpg`); `twitter:card` = `summary_large_image` on all 4. **Ran `npm run test` myself — 23/23 passed.** Brand-language grep: `seo.ts` has zero "high end" hits; one substring hit for "tailor" at line 68 (`knowsAbout: 'Tailoring craft'`) plus the law comment itself at line 13 — judged NOT a violation: `brand.md` (canonical brand facts) itself lists "tailoring" as a core product/value term, and the actual rule as stated in `decisions.md`/`sitemap/collections.md` is about never *calling the maison* "a tailor", not banning the substring outright; `Seo.astro` has zero hits for either term. Titles/descriptions checked against `01_seo_landscape.md`: home page uses "Bespoke Áo Dài", "Occasion Wear", "Saigon" (Pillar A/geo positioning); collections page names "Renaissance — Spring Summer 2026" (matches the SS26 press narrative in `03_entity_mentions.md`); booking page uses on-brand, lawful language. All 5 flagged worker deviations (og:image = collection hero, no `@astrojs/sitemap` dependency, `mentions` included alongside `sameAs`/`subjectOf` per D-SEO-01's broader wording, trailing-slash normalization on sitemap/breadcrumb URLs, no canonical on 404) are documented in this log entry and are each consistent with `decisions.md`. Note for the record (not a defect): D-SEO-01's decision text says "Organization (+ClothingStore/LocalBusiness)" but the implementation (and this verification's own scope contract, which explicitly named `["Organization","ClothingStore"]`) omits `LocalBusiness` as a literal `@type` — schema.org's `ClothingStore` is itself a `LocalBusiness` subtype so this is not a functional gap, but the decisions.md wording is broader than what was built; flagging as a doc-precision open item, not a fail.

---

## 2026-07-03 — D-SCOPE-03 /collections static page built from IG export — awaiting pm verify

- **Assets:** 11 curated JPGs copied from the IG export to `src/assets/collections/renaissance/` with kebab names (hero-white-jacquard-aodai, runway-velvet-noir-duo, runway-pink-sequin-halter, muse-gigi-sequin-floral-stage, muse-gigi-pink-constellation, muse-tu-anh-blush-lace, muse-alita-black-lace-blossom, muse-eastern-forest-embroidered-coat, muse-thu-anh-black-sequin-mandarin, manifesto-renaissance-brocade, manifesto-renaissance-jacquard). Every file visually opened (Read tool) before wiring, per spec.
- **Curation DEVIATIONS (2, both within the same post per the spec's own substitution rule):** (1) post 011: spec's `_004` slide is a full-bleed photo with NO white frame/lockup → substituted `011_001` (branded white-frame card, blush lace áo dài, MAISON DENUDE lockup). (2) post 013: spec's `_002` slide is a candid gallery shot with no branding → substituted `013_001` (only slide in post 013 carrying the MAISON DENUDE lockup; embroidered coat back). NOTE: manifesto pick `001_001` carries the « RENAISSANCE » display type in-artwork but not the MAISON DENUDE wordmark (its pair `002_001` carries the wordmark) — kept as locked since the two are halves of one pinned title spread and posts 001's other slides are pure text cards.
- **Registry:** 11 new ImageKeys (`ren*` prefix) added to `src/lib/assets.ts` following the existing import/union/map idiom.
- **Content:** new typed `src/content/collections.ts` — `Collection` model (slug/title/season/showCredit/hero/manifesto/manifestoImages/sections/cta); ALL copy + image refs live there. Manifesto = 3 strongest English lines from posts 001/002 caption ("Every chapter is about presence…", "A woman of Maison Denude is a Renaissance woman.", "She chooses, savours, and becomes.").
- **Components (new, additive):** `src/components/collections/` — CollectionHeader, CollectionHero (centered, floats on black, NOT full-bleed), ManifestoStrip, SectionLabel, PairRow (RUNWAY 2-up), MuseGrid (asymmetric 2/3/1 row rhythm, name captions), ClosingCta (thin rule + BOOK A PRIVATE CONSULTATION → /booking). All `fit="contain"` so the cards' own white frames are never cropped. Zero edits to existing section components.
- **Page:** new `src/pages/collections.astro` (`prerender = true`, Base + Nav + Footer; footer data reused from `landing.ts` single source). Title `Renaissance — Spring Summer 2026 | Maison Denude`; description uses bespoke/artisanal vocabulary, no banned brand language.
- **Nav:** `src/content/nav.ts` COLLECTION href flipped `#collection` → `/collections`.
- **Tokens:** 4 added to `tokens.css` under a `Collections page (D-SCOPE-03)` comment: `--label-line-gap: 4px`, `--collection-hero-max: 720px`, `--collection-manifesto-max: 640px`, `--collection-rule-width: 48px`.
- **Verify:** `npm run build` exit 0, `/collections/index.html` prerendered; all 11 assets appear hashed in `dist/client/_astro/` and referenced in the page HTML (no placeholder fallbacks); grep of new components → no hex colors, no gradients/shadows/border-radius/scale; remaining px are media-query breakpoints + `sizes` attrs (same idiom as existing PhotoRow/FeatureCollection) and the 1px hairline rule (matches Footer's existing `1px solid var(--color-rule)` pattern).
- **Not touched (per scope):** landing sections, booking/email code, Hero.astro WIP, wrangler.jsonc, SEO/JSON-LD, Supabase, video.
- **pm VERIFY (2026-07-03): PASS.** Verified against `sitemap/collections.md`, `decisions.md` D-SCOPE-03 + D-ARCH-01, and `design-system.md`. (1) Confirmed 11 JPGs in `src/assets/collections/renaissance/`, all registered as `ren*`-prefixed `ImageKey`s in `src/lib/assets.ts` following the existing import/union/map idiom (grepped — no raw paths). (2) `src/content/collections.ts` read in full: typed `Collection` model holds title/season/showCredit/manifesto/manifestoImages/sections/cta; muse captions in exact spec order (GIGI HƯƠNG GIANG ×2, TÚ ANH, ALITA, EASTERN FOREST, THU ANH); grepped collections.ts + all 7 new components for "high end"/"tailor" — zero hits. (3) New components confirmed present only under `src/components/collections/` (7 files); `collections.astro` has `export const prerender = true`; `git diff` on `src/components/sections/` shows only Footer.astro's already-verified P1 blog-link removal and an unrelated Hero.astro WIP change (pre-existing, flagged in CONTINUITY.md) — zero edits to existing section components from this phase. (4) `nav.ts` diff confirms `COLLECTION` href now `/collections`. (5) `tokens.css` diff shows exactly 4 new tokens under a `Collections page (D-SCOPE-03)` comment; grepped all new components for hex colors, gradient, box-shadow, border-radius, `scale(` — zero hits; cross-checked every token referenced in the new components resolves to a real token in tokens.css. (6) Page structure read top-to-bottom in `collections.astro`: CollectionHeader → CollectionHero (018 hero) → ManifestoStrip → sections.map (PairRow=RUNWAY, MuseGrid=MUSES) → ClosingCta → Footer — matches spec order exactly; MuseGrid implements a 2/3/1 repeating asymmetric rhythm (not a uniform grid), CollectionHero uses `fit="contain"` (not full-bleed cover) so the cards' white frames float on black per spec. (7) Both logged curation deviations (011_001 substituted for 011_004; 013_001 substituted for 013_002) and the manifesto 001/002 wordmark note are present in this log entry and consistent with the spec's own substitution rule ("if one is not [branded], pick the next slide... and note the deviation"). (8) Visually opened (Read tool) `hero-white-jacquard-aodai.jpg`, `muse-tu-anh-blush-lace.jpg`, `muse-eastern-forest-embroidered-coat.jpg` — all three are branded white-frame editorial cards carrying the MAISON DENUDE lockup, not candid shots. No `npm` build/install run per scope constraint (file inspection, grep, git diff, image reads only).

---

## 2026-07-03 — SEO entity-mention research complete → `context/SEO/03_entity_mentions.md` (sameAs: 4 official profiles + unclaimed Google Business listing found; subjectOf: 6 verified press URLs incl. Tuổi Trẻ/NLĐ, Báo Văn Hóa, VTV, CafeBiz, vietnam.vn, ELLE Decoration; TikTok @maison.denude + Threads @maisondenude.official newly discovered; nld.com.vn article now 301s to tuoitre.vn; daaninterior.com.vn flagged as name-collision, excluded) — pm PASSED

- **pm VERIFY (2026-07-03): PASS.** Verified against `D-SEO-01` (frontend/decisions.md, Session 2026-07-03) and the report's own stated structure/method. Confirmed all required sections present: summary, official-profiles table, press/editorial table (URL/domain-tier/date/lang/context/verification per row), KOL/social section, name-collision/excluded section (daaninterior.com.vn correctly flagged as a different entity), and fenced plain-URL "Recommended JSON-LD arrays" for `sameAs`/`subjectOf`. `sameAs` block contains exactly the 4 expected official URLs (facebook.com/MaisonDenude, instagram.com/maisondenude.official, tiktok.com/@maison.denude, threads.com/@maisondenude.official) — no press/KOL/third-party leakage. `subjectOf` block: 6 URLs, all verified or explicitly caveated (vietnam.vn / elledecoration.vn 403 bot-wall noted); old nld.com.vn URL correctly absent from the recommended block (only referenced in prose to explain the 301). Cross-checked KOL seed doc `C:\maison\social proof maison denude.md` — Chi Pu, Văn Mai Hương, Cô Em Trendy, The Planners all present in the report. Spot-checked 3 `subjectOf` URLs live: tuoitre.vn (200, browser UA, content confirmed "Maison Denude" on page), cafebiz.vn (200 with browser UA — plain curl got 503 from bot-protection, consistent with report's live-but-bot-walled note), vietnam.vn (200 with browser UA, "Maison Denude" appears 5+ times in body) — all genuinely about the fashion brand. Added the two required DOC CRUD items (pm-authored, not the original worker's scope): SimilarWeb-no-data note and explicit "unclaimed KG/Business = top recommended action" framing, both now in the "Open items / gaps" section.

---

## 2026-07-03 — D-BOOK-02 booking email via Resend (TDD) — pm PASSED

- **Test infra:** `vitest@4.1.9` installed (dev-only, NO Resend SDK — transport is raw `fetch` to Resend REST API, Workers-native). `package.json` gained `"test": "vitest run"`. New `vitest.config.ts` scoped to `src/**/*.test.ts`, node environment.
- **Tests first:** `src/lib/email.test.ts` — 23 tests written BEFORE the implementation, covering: from/to/subject/html/text shape (sender `Maison Denude <bookings@contact.maisondenude.com>`), customer name in subject, all booking fields in html+text, Vietnamese diacritics survival, empty/null/undefined notes, all 3 consultationType values, HTML-escaping of `<script>`/`&` (injection safety), banned brand-language check ("high end"/"tailor"), and sender behavior via injected mock fetch: 200 → `{ok:true,id}`; 4xx/5xx → `{ok:false,error}` no-throw; network reject → `{ok:false}` no-throw; missing/empty API key → `{ok:false}` + fetch never called; request verified (POST https://api.resend.com/emails, `Authorization: Bearer`, JSON body === buildBookingEmail output). No test reads the real API key; no live API call anywhere.
- **Implementation:** `src/lib/email.ts` stub replaced — pure `buildBookingEmail(booking, recipient?)` (plain B/W HTML details table, no external assets, no banned copy), `sendBookingNotification(booking, {apiKey, recipient, fetchImpl?})` (never throws), `resolveEmailConfig(env)` (reads `RESEND_API_KEY` + `BOOKING_NOTIFY_EMAIL`, defaults `thieuxmaison@gmail.com`). Zero import-time env access. Exported seam name `sendBookingNotification` kept (D-BOOK-01) — signature now takes injected deps.
- **Wiring:** `src/actions/index.ts` `createBooking` — after successful Supabase insert, `sendBookingNotification(input, resolveEmailConfig(env))`; on `!ok` → `console.error` and STILL return success (insert first, email best-effort per D-BOOK-02). No other action changes.
- **Env:** `BOOKING_NOTIFY_EMAIL=thieuxmaison@gmail.com` appended to `main-dev/.env`. wrangler.jsonc NOT touched (uncommitted WIP) — **deploy phase must run `wrangler secret put RESEND_API_KEY` and `wrangler secret put BOOKING_NOTIFY_EMAIL`** (or [vars] for the latter) before the live smoke test.
- **Verify:** `npm run test` → 1 file, 23/23 passed. `npx astro build` → exit 0, all 3 routes prerendered.
- **Not touched (per scope):** BookingForm.astro/UI, nav/footer/landing/collections, Hero.astro WIP, wrangler.jsonc, SEO/meta, no deploy, no real email sent.
- **pm VERIFY (2026-07-03): PASS.** Verified against `decisions.md` D-BOOK-02 + D-BOOK-01 seam + D-ARCH-01, and `sitemap/booking.md`. `package.json` diff confirms `vitest@^4.1.9` dev-only + `"test": "vitest run"`, NO resend SDK dependency (grepped package.json and package-lock.json — zero hits); `vitest.config.ts` scoped to `src/**/*.test.ts`, node env. `email.test.ts` ran live: `npm run test` → 1 file, **23/23 passed** (15 static `it`/`it.each` call sites expand to 23 runtime cases via the notes/consultationType/status-code `.each` tables) — confirms sender `Maison Denude <bookings@contact.maisondenude.com>`, default recipient `thieuxmaison@gmail.com`, Vietnamese diacritics preserved, empty/null/undefined notes handled without "undefined"/"null" leaking, all 3 consultation types, HTML-escaping of `<script>`/`&`, banned brand-language guard, and `sendBookingNotification` non-throwing on 200/4xx/5xx/network-reject/missing-key with exact request assertions (POST `https://api.resend.com/emails`, `Bearer` auth, JSON body === `buildBookingEmail` output). Grepped `email.test.ts` for `process.env`/real key material — none found; only literal test fixtures (`re_test_123`, `re_test_key`) are used, so no test path can touch the real Resend key or make a live network call. `email.ts` read in full: zero import-time env access, `resolveEmailConfig(env)` reads `RESEND_API_KEY` + `BOOKING_NOTIFY_EMAIL` (default `thieuxmaison@gmail.com`), seam name `sendBookingNotification` preserved per D-BOOK-01. `git diff src/actions/index.ts` shows a minimal, surgical addition (one import + the email-send block after the insert, inside the existing handler) — email is called only after the Supabase insert succeeds, via `env` from `cloudflare:workers`, failure is `console.error`-logged but action still returns `{ success: true }`; no other restructure. `.env` diff confirms only `BOOKING_NOTIFY_EMAIL=thieuxmaison@gmail.com` appended; `git diff wrangler.jsonc` shows only the pre-existing uncommitted `main-dev`→`maisondenude-web` rename already flagged as WIP in `CONTINUITY.md`, not touched by this phase. `npm run test` executed directly by pm in `main-dev/` (no build/install run, per scope) — all green.

---

## 2026-07-03 — D-NAV-01 nav/footer cleanup + socials + 404 page — pm PASSED

- **Nav (D-NAV-01):** `src/content/nav.ts` — SOCIAL CLUB, FEATURING, BLOG items commented out with `// hidden per D-NAV-01 — restore when content exists`. Nav now = INTRODUCTION (#introduction) · COLLECTION (#collection) · BOOKING (/booking). COLLECTION href unchanged per scope.
- **Footer:** `src/components/sections/Footer.astro` — `/blog` link removed from bottom bar; © line + address + Booking link kept, no restructure.
- **Socials:** `src/content/landing.ts` — Facebook → `https://www.facebook.com/MaisonDenude`, Instagram → `https://www.instagram.com/maisondenude.official`. **Zalo entry removed entirely (no URL exists yet) — OPEN ITEM: re-add when official Zalo link confirmed.**
- **New page:** `src/pages/404.astro` — `prerender = true`, Base + Nav, token-only styles (no design literals), uppercase/wide-tracking house copy ("404 / PAGE NOT FOUND" + return-home link), code style matches booking.astro.
- **Verify:** `npm run build` exit 0 — /404.html, /booking/index.html, /index.html all prerendered. Grep `src/` for `href="#"` / `href: '#'` → zero matches. Grep `/blog` → only the commented-out nav.ts line (intentional per scope).
- **Not touched (per scope):** Hero.astro WIP, wrangler.jsonc, booking/email code, landing sections, astro.config.mjs, SEO/meta.
- **pm VERIFY (2026-07-03): PASS.** Verified against `decisions.md` D-NAV-01 (Session 2026-07-03) and design-system.md conventions. `nav.ts` diff confirms SOCIAL CLUB/FEATURING/BLOG commented with `hidden per D-NAV-01` tag, live array = INTRODUCTION/COLLECTION/BOOKING; `Nav.astro` maps the array on both desktop rail and mobile overlay, so commented items render nowhere (no dead links). `Footer.astro` diff confirms only the `/blog` `<a>` removed, © + address + Booking link untouched. `landing.ts` diff confirms Facebook/Instagram URLs swapped in, Zalo entry removed with an inline comment (documented deviation, not silent). `404.astro` has `prerender = true`, uses `Base` + `Nav`, every style value is `var(--token)` (all tokens exist in `tokens.css`), copy is on-brand uppercase house style, link returns to `/`. Grepped `src/` for `href="#"` / `href: '#'` — zero matches; grepped for `/blog` — only the intentional commented nav.ts line. `git diff --stat` confirms only the 4 claimed files plus pre-existing WIP (Hero.astro, wrangler.jsonc) and PM-owned context docs changed; new untracked `sitemap/collections.md` belongs to the parallel D-SCOPE-03 effort, not this phase. Invariant 4 (every page declares prerender) holds across index/404/booking.
- Next: orchestrator may proceed to next phase (booking email TDD / collections / SEO per CONTINUITY.md next actions).

---

## 2026-06-26 — Prod go-live: domain connected, all tracking verified

- **Domain:** maisondenude.com connected to the thieuxmaison CF Worker — site is now live on prod domain.
- **GTM:** ✅ confirmed firing on maisondenude.com.
- **GA4:** ✅ property created, Measurement ID wired, receiving live data.
- **Microsoft Clarity:** ✅ project created, confirmed live via GTM.
- **Google Search Console:** ✅ domain property verified for maisondenude.com.
- **Stray Worker cleanup (D-DEPLOY-03):** ✅ stray `main-dev` Worker in nguyenthaithieu account deleted.
- **OQ-002 resolved:** domain = maisondenude.com (was open since kickoff).
- **Context updated:** CONTINUITY.md, frontend/decisions.md tracking table, open_questions.md OQ-002.
- **Where we are in plan.md:** Phase 0 ✅ (infra + tracking), Phase 1 tracking items ✅ — remaining Phase 1 items (Google Business Profile, keyword baseline) + Phase 2 remaining (booking email, booking modal) are next.

---

## 2026-06-24 — Phase 5 COMPLETE — pm PASSED (live booking insert verified)

- **Code built + pm-verified PASS:** `src/lib/supabase.ts` (supabaseAdmin + supabasePublic factories — invariant 6 held, createClient only here), `src/lib/email.ts` (sendBookingNotification no-op stub, NOT called — D-BOOK-01 email half deferred), `src/actions/index.ts` (createBooking action: accept:'form', astro:schema zod input, supabaseAdmin insert, camelCase→snake_case field mapping, ActionError INTERNAL_SERVER_ERROR on failure), `src/components/ui/BookingForm.astro` (PE form, no-JS, actions.createBooking, success/error states), `src/pages/booking.astro` (prerender=true, Base + Nav). 9 form tokens added to tokens.css.
- **Supabase `bookings` table:** created via Supabase MCP `apply_migration` (NOT the pooler — pooler returned "tenant not found"; MCP apply_migration worked). Schema per sitemap/booking.md spec; RLS on.
- **Worker secrets:** SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_ANON_KEY set + bound — verified via `wrangler secret list`.
- **Deployed to:** https://main-dev.thieuxmaison.workers.dev.
- **BUG FOUND + FIXED during live gate:** action used `Astro.locals.runtime.env` which was removed in Astro v6+ (project is Astro 7 / adapter v14) → 500 "Astro.locals.runtime.env has been removed". Fix: read Worker env via `import { env } from 'cloudflare:workers'` in `src/actions/index.ts` (handler simplified to `(input)` signature with no second arg). This is the canonical env-access pattern for this adapter — reusable lesson alongside the Phase 4 build-time-vs-runtime lesson.
- **LIVE GATE PASSED:** same-origin FormData POST to `/_actions/createBooking` → `{"success":1}`; row confirmed in Supabase via MCP (all fields correct, created_at auto-set); test row deleted (table back to 0 rows).
- **Browser note:** page is served at `/booking/` (trailing slash; `/booking` 307-redirects). Astro CSRF (checkOrigin) blocks cross-origin curl without an Origin header — real browser submits are fine.

## 2026-06-24 — Phase 5 (booking, insert-only) BUILT — awaiting pm verify
- Built per sitemap/booking.md: `src/lib/supabase.ts` (supabaseAdmin + supabasePublic factories — invariant 6, the only place createClient lives), `src/lib/email.ts` (sendBookingNotification no-op stub, NOT called — D-BOOK-01 email half deferred), `src/actions/index.ts` (createBooking action: accept:'form', astro:schema zod input, supabaseAdmin insert, camelCase consultationType → snake_case consultation_type, ActionError INTERNAL_SERVER_ERROR on failure), `src/components/ui/BookingForm.astro` (PE form, no-JS, actions.createBooking, getActionResult success/error states), `src/pages/booking.astro` (prerender=true, Base + Nav).
- Astro 7.0.2 auto-enables actions via src/actions/index.ts `server` export — NO config flag added (verified).
- env read from `context.locals.runtime.env` (Workers), not process.env.
- Build: `npm run build` exit 0; both /booking and / prerendered. Invariant 6 grep: createClient only in lib/supabase.ts. No hardcoded design literals in new .astro <style> blocks.
- Tokens ADDED to tokens.css (form fields): --form-max-width, --form-field-gap, --form-label-gap, --field-padding-y, --field-border, --field-border-focus, --text-field, --text-label, --ls-label.
- Deviation note: BookingForm has one UA-pseudo-element rule `::-webkit-calendar-picker-indicator { filter: invert(1) }` to make the native date picker icon visible on black bg — a browser-control normalization, not a brand design value (no token applies).
- NOT done (orchestrator-owned): deploy, Worker secrets, live-submit insert gate.
- Next: orchestrator routes through @pm verify, then deploy + live submit.

## 2026-06-24 — Layout/typography foundational fix — pm PASSED
- (a) **Font (FQ-I-01 RESOLVED):** ABChanel Corpo woff2 files located in `context/reference/`; wired via `@font-face` in `global.css` (Light→200/300, Regular→400, `font-display:swap`); preload link added in `Base.astro`; files copied to `public/fonts/`. Font served HTTP 200 and preload confirmed live at https://main-dev.thieuxmaison.workers.dev.
- (b) **Image-distortion fix:** Root cause identified as astro:assets injecting intrinsic `width`/`height` attributes conflicting with `CSS width:100%` + no height reset. Fixed foundationally in `SmartImage.astro` via a `fit: cover|contain` contract. Grid cell containers use `aspect-ratio:3/4`; feature-strip container uses `contain`. Each container owns its box; no global override.
- Build clean (exit 0), deployed successfully.
- Note: 4 images still render `_placeholder` — content-config keys not pointing at real files; non-blocking, intended fallback behavior.

## 2026-06-24 — Phase 4 COMPLETE — pm PASSED (tracking confirmed live)
- D-DEPLOY-03 fixed: account_id `73fb68b2979b1b17abbafc4eccdbc354` (Thieuxmaison) pinned in wrangler.jsonc.
- Deployed to https://main-dev.thieuxmaison.workers.dev — exit 0.
- GTM fix: PUBLIC_GTM_ID must be a build-time var (prerendered page), not a Worker secret. Added to .env + wrangler.jsonc [vars]. Rebuilt + redeployed. GTM confirmed working by user.
- Clarity auto-connects via GTM (no separate snippet needed).
- GA4 + Clarity IDs pending PT-02/PT-03 — add to .env + wrangler.jsonc vars when ready, rebuild + redeploy.
- Stray Worker in nguyenthaithieu account: delete manually via CF dashboard (wrangler CLI can't cross-account delete).
- Next: Phase 5 — booking (insert-only).

## 2026-06-24 — Phase 3 COMPLETE — pm PASSED
- Content layer + sections in place: types.ts (Section union), landing.ts (9 rows — 8 sections + footer), nav.ts, SectionRenderer.astro, Hero/PhotoRow/FeatureCollection/VideoSection/Footer, Nav/Logo/SocialIcons, index.astro (prerender=true).
- npm build: exit 0. All 6 change-tolerance invariants PASS.
- Acceptable deviations: 24px overlay nav font-size (no token, spec names it explicitly), 64px play button (structural), 80px footer inner padding (mirrors --gap-section, no alias token yet).
- Next: Phase 4 — deploy to thieuxmaison CF account, fix D-DEPLOY-03, verify GTM/GA4/Clarity live.

## 2026-06-24 — Phase 2 COMPLETE — pm PASSED
- Foundation layer in place: astro.config.mjs (output server + imageService compile), tokens.css, global.css, Base.astro (GTM+Clarity conditional injection, CSS via frontmatter import), lib/assets.ts (ImageKey registry + placeholder fallback), SmartImage.astro.
- @supabase/supabase-js installed (^2.108.2).
- npm build: exit 0. Fix-loop: FAIL (broken CSS link) → fixed (frontmatter import) → PASS.
- Next: Phase 3 — Content layer + section components (general agent).

## 2026-06-24 — Phase 1 COMPLETE — pm PASSED
- Asset pipeline done: 30 files in `main-dev/src/assets/landing/`, logos in `public/`.
- 3 HEIC files converted to JPG via magick (kol-placeholder-1/2/3.jpg).
- 1 duplicate skipped per spec (Section 2 hero duplicate).
- Next: Phase 2 — Foundation (general agent).

## 2026-06-24 — Phase 0 COMPLETE — pm PASSED
- pm gate: first pass FAIL (3 items) → fixed → re-verify PASS.
- Fixes: created `management/task.md` (phase tracker); clarified `sitemap/booking.md` email note as a phase-deferral (not a D-BOOK-01 override); updated `CONTINUITY.md` to Phase 0 done + refreshed "Last pm-PASSED state".
- Harness now in place: `frontend/decisions.md` (legal), `frontend/sitemap/{_index,landing,booking}.md` (buildable specs), `management/{CONTINUITY,status_log,task}.md`.
- Used existing `context/management/` (not a new frontend/management) per user correction.
- **Next:** user is compacting. On resume → read `management/CONTINUITY.md` + `sitemap/landing.md` + `sitemap/booking.md` + `frontend/decisions.md`, then spawn bull for Phase 1.

## 2026-06-24 — Phase 0 started: doc reorganization
- Brainstorm + plan session completed. Plan approved: `C:\Users\Thieu\.claude\plans\dynamic-snacking-pond.md`.
- Logged binding decisions D-DEPLOY-01/02/03, D-SCOPE-01/02, D-DOMAIN-01, D-DOC-01, D-ASSET-01, D-BOOK-01, D-ARCH-01 in `decisions.md`.
- Renamed `decisions-log.md` → `decisions.md` (legal source of truth).
- Created `management/{CONTINUITY,status_log,task}.md` and `sitemap/{_index,landing,booking}.md`.
- Discovered: `logo_white.svg` already exists; CF adapter defaults to SSR (landing must opt into prerender); 3 HEIC + 18 spaced + 2 paren filenames need cleanup.
